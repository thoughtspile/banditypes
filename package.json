{
  "name": "banditypes",
  "version": "0.2.5",
  "description": "The might 400-byte schema validator",
  "author": "Vladimir Klepov v.klepov@gmail.com",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "module": "./dist/banditypes.js",
  "main": "./dist/banditypes.cjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/banditypes.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/banditypes.cjs"
      }
    }
  },
  "imports": {
    "#lib": "./dist/banditypes.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": "^14 || ^16 || >=18"
  },
  "scripts": {
    "test:types": "tsc && tsc -p tests --noEmit",
    "test:runtime": "vite build && node --test tests",
    "test": "npm run test:types && npm run test:runtime",
    "test:coverage": "c8 --100 --exclude tests npm test",
    "lint": "prettier --check --log-level warn .",
    "lint:fix": "prettier --loglevel silent --write .",
    "size": "node bench/run",
    "build:types": "tsc && cp dist/index.d.ts dist/index.d.cts",
    "build": "vite build && npm run build:types",
    "publint": "npm run build && npx publint && npx -y @arethetypeswrong/cli --pack"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thoughtspile/banditypes.git"
  },
  "keywords": [
    "typescript",
    "superstruct",
    "schema",
    "validation",
    "zod"
  ],
  "bugs": {
    "url": "https://github.com/thoughtspile/banditypes/issues"
  },
  "homepage": "https://github.com/thoughtspile/banditypes#readme",
  "devDependencies": {
    "@types/node": "^18.14.1",
    "c8": "^10.1.3",
    "esbuild": "^0.25.8",
    "expect-type": "^1.1.0",
    "nano-staged": "^0.8.0",
    "prettier": "^3.4.2",
    "publint": "^0.3.12",
    "simple-git-hooks": "^2.11.1",
    "superstruct": "^2.0.2",
    "terser": "^5.37.0",
    "typescript": "^5.7.2",
    "vite": "^6.3.5",
    "zod": "^3.24.1"
  },
  "nano-staged": {
    "*": "prettier --loglevel silent --write"
  },
  "simple-git-hooks": {
    "pre-commit": "./node_modules/.bin/nano-staged"
  }
}
